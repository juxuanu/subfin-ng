[tools]
dotnet = "10"
python = "3.12"  # the conformance suite's scripts (run through uv)
uv = "0.12"

[env]
UV_PYTHON_PREFERENCE = "only-system"  # uv runs them on the Python above, not one it downloads
